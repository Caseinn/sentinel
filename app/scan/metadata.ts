import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Scan URL",
  description: "Scan any URL for security vulnerabilities including leaked secrets, misconfigured headers, cookie issues, and more.",
  robots: {
    index: true,
    follow: true,
  },
};
